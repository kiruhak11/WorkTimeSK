# WorkTimeSK - Система управления расписанием

Полноценная система для управления расписанием сотрудников с веб-интерфейсом и Telegram ботом.

## 🚀 Возможности

- 📅 Управление расписанием сотрудников через веб-интерфейс
- 🤖 Регистрация сотрудников через Telegram бота
- 📊 Автоматический расчет рабочих часов
- 📱 Отправка уведомлений о расписании в Telegram
- 📥 Экспорт расписания в XLS файл
- 🎨 Цветовое кодирование по времени начала смены
- 🐳 Готовые Docker контейнеры для развертывания

## 🛠 Технологии

### Backend & Frontend

- **Nuxt 3** - фреймворк для веб-приложения
- **Vue 3** - UI фреймворк
- **TypeScript** - типизация
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - база данных
- **SCSS** - стилизация (без Tailwind)
- **XLSX** - экспорт в Excel

### Telegram Bot

- **Python 3.11** - язык программирования
- **python-telegram-bot** - библиотека для работы с Telegram API
- **requests** - HTTP запросы к API

### DevOps

- **Docker** - контейнеризация
- **Docker Compose** - оркестрация контейнеров

## 📋 Предварительные требования

- Node.js 20+
- Python 3.11+
- Docker и Docker Compose (для запуска в контейнерах)
- PostgreSQL (если запускаете локально без Docker)
- Telegram Bot Token (получить у @BotFather)

## 🏃 Быстрый старт с Docker

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd WorkTimeSK
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp env.example .env
```

Отредактируйте `.env` и укажите ваш Telegram Bot Token:

```env
DATABASE_URL="postgresql://postgres:postgres@db:5432/worktimesk?schema=public"
TELEGRAM_BOT_TOKEN="your_bot_token_here"
SECRET_REGISTRATION_CODE="1234"
API_BASE_URL="http://web:3000"
```

Создайте файл `.env` для бота:

```bash
cd bot
cp env.example .env
```

И укажите те же данные:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
API_BASE_URL=http://web:3000
SECRET_REGISTRATION_CODE=1234
```

### 3. Запуск приложения

```bash
# Вернитесь в корень проекта
cd ..

# Запустите все контейнеры
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

Приложение будет доступно по адресу: `http://localhost:3000`

### 4. Остановка приложения

```bash
docker-compose down
```

## 💻 Локальная разработка (без Docker)

### 1. Установка зависимостей

```bash
# Установка зависимостей веб-приложения
npm install

# Установка зависимостей бота
cd bot
python3 -m venv lab_venv
source lab_venv/bin/activate
pip install -r requirements.txt
cd ..
```

### 2. Настройка базы данных

Убедитесь, что PostgreSQL запущен и создайте базу данных:

```sql
CREATE DATABASE worktimesk;
```

Обновите `.env` с параметрами вашей локальной БД.

### 3. Применение миграций

```bash
npm run prisma:deploy
# или для разработки
npm run prisma:migrate
```

### 4. Запуск приложений

В одном терминале запустите веб-приложение:

```bash
npm run dev
```

В другом терминале запустите бота:

```bash
cd bot
python bot.py
```

## 📖 Использование

### Регистрация сотрудника

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Следуйте инструкциям бота:
   - Введите имя
   - Введите фамилию
   - Выберите должность
   - Введите секретный код (по умолчанию: `1234`)

### Создание расписания

1. Откройте веб-интерфейс (`http://localhost:3000`)
2. Выберите неделю
3. Выберите сотрудника из списка
4. Заполните расписание на неделю в формате:
   - `12-24` - работа с 12:00 до 24:00
   - `16-2` - работа с 16:00 до 02:00 (через полночь)
   - `выходной` или `в` - выходной день
5. Нажмите "Сохранить расписание"

### Редактирование расписания

- Кликните на ячейку в таблице расписания
- Введите новое время или "выходной"
- Нажмите "Сохранить"

### Подтверждение и отправка

1. После заполнения всех расписаний нажмите "Подтвердить и отправить"
2. Все сотрудники получат уведомления в Telegram с их расписанием

### Экспорт в Excel

Нажмите кнопку "Экспорт в XLS" для скачивания таблицы в формате Excel

## 🎨 Цветовое кодирование

- 🟢 **Зеленый** - смена с 10:00 и раньше
- 🔵 **Синий** - смена с 11:00-12:00
- 🔴 **Красный** - смена с 13:00-14:00
- 🟡 **Желтый** - смена с 16:00-17:00 и позже
- ⚫ **Черный** - выходной день

## 📁 Структура проекта

```
WorkTimeSK/
├── app/                      # Главный файл приложения
├── assets/                   # Стили SCSS
│   └── styles/
│       ├── main.scss
│       └── variables.scss
├── bot/                      # Telegram бот
│   ├── bot.py
│   ├── requirements.txt
│   └── Dockerfile
├── components/               # Vue компоненты
│   ├── ScheduleForm.vue
│   └── ScheduleTable.vue
├── pages/                    # Страницы приложения
│   └── index.vue
├── prisma/                   # Схема БД и миграции
│   ├── schema.prisma
│   └── migrations/
├── public/                   # Статические файлы
├── server/                   # Backend API
│   ├── api/
│   │   ├── auth/            # Регистрация
│   │   ├── users/           # Управление пользователями
│   │   ├── schedules/       # Управление расписаниями
│   │   └── export/          # Экспорт в XLS
│   └── utils/
│       └── prisma.ts        # Prisma клиент
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile               # Dockerfile для веб-приложения
├── nuxt.config.ts           # Конфигурация Nuxt
└── package.json             # Зависимости

```

## 🔧 Скрипты NPM

```bash
# Разработка
npm run dev                  # Запуск dev сервера

# Сборка
npm run build                # Сборка для продакшена
npm run preview              # Предпросмотр production сборки

# Prisma
npm run prisma:generate      # Генерация Prisma клиента
npm run prisma:migrate       # Создание и применение миграций
npm run prisma:deploy        # Применение миграций в продакшене
npm run prisma:studio        # Открыть Prisma Studio

# Docker
npm run docker:build         # Сборка Docker образов
npm run docker:up            # Запуск контейнеров
npm run docker:down          # Остановка контейнеров
npm run docker:logs          # Просмотр логов
```

## 🔒 Безопасность

- Измените секретный код регистрации в `.env`
- Не коммитьте файлы `.env` в Git
- Используйте сильные пароли для БД в продакшене
- Храните Telegram Bot Token в секрете

## 🐛 Решение проблем

### Проблемы с портами

Если порт 3000 или 5432 уже занят, измените их в `docker-compose.yml`:

```yaml
ports:
  - "3001:3000" # Внешний порт : Внутренний порт
```

### Бот не отвечает

1. Проверьте правильность TELEGRAM_BOT_TOKEN
2. Проверьте логи бота: `docker-compose logs bot`
3. Убедитесь, что контейнер web запущен и доступен

### Ошибки миграций БД

```bash
# Пересоздание БД
docker-compose down -v
docker-compose up -d
```

## 📝 API Endpoints

### Пользователи

- `POST /api/auth/register` - Регистрация пользователя
- `GET /api/users` - Список всех пользователей
- `GET /api/users/:id` - Получить пользователя по ID

### Расписания

- `POST /api/schedules` - Создать/обновить расписание
- `GET /api/schedules` - Получить расписания (с фильтрами)
- `PATCH /api/schedules/:id` - Обновить расписание
- `POST /api/schedules/confirm` - Подтвердить и отправить уведомления

### Экспорт

- `GET /api/export/schedule` - Экспорт расписания в XLS

## 🤝 Вклад в проект

Если вы хотите внести свой вклад:

1. Сделайте Fork проекта
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан для внутреннего использования.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

---

Создано с ❤️ для управления расписанием курьеров

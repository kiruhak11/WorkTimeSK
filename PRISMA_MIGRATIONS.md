# Инструкция по применению миграций Prisma

## 📋 Применение существующей миграции

### В Docker (продакшен/сервер):

```bash
# Если контейнеры запущены
docker-compose exec web npx prisma migrate deploy

# Или пересоздать контейнер (миграции применятся автоматически при запуске)
docker-compose down
docker-compose up -d --build web
```

### Локально (разработка):

```bash
# Применить миграции
npm run prisma:deploy

# Или для разработки (создаст миграцию если есть изменения)
npm run prisma:migrate
```

## 🔧 Создание новой миграции

Если вы изменили `schema.prisma` и нужно создать миграцию:

### 1. Измените схему в `prisma/schema.prisma`

### 2. Создайте миграцию:

```bash
# Для разработки - создаст миграцию и применит её
npm run prisma:migrate

# Или через npx напрямую
npx prisma migrate dev --name название_миграции
```

### 3. После создания миграции:

```bash
# Сгенерируйте Prisma Client
npm run prisma:generate
```

## 🚀 Быстрое применение миграции для department

Для применения уже созданной миграции `20250125000000_add_department`:

### Вариант 1: Через Docker

```bash
cd /var/www/html/worktimesk

# Применить миграции
docker-compose exec web npx prisma migrate deploy

# Или перезапустить контейнер
docker-compose restart web
```

### Вариант 2: Локально

```bash
# Убедитесь, что DATABASE_URL правильно настроен в .env
npm run prisma:deploy
```

## 📝 Текущая миграция

Миграция `20250125000000_add_department` добавляет поле `department` в таблицу `users`:

```sql
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "department" TEXT NOT NULL DEFAULT 'штат';
```

Это поле:
- Добавляется в таблицу `users`
- Имеет значение по умолчанию `'штат'`
- Использует `IF NOT EXISTS` для безопасного выполнения

## ⚠️ Важно

- Миграция использует `IF NOT EXISTS`, поэтому безопасна для повторного запуска
- Существующие пользователи получат значение `'штат'` по умолчанию
- Новые пользователи смогут выбирать между 'кухня' и 'штат'

## 🔍 Проверка состояния миграций

```bash
# Посмотреть статус миграций
npx prisma migrate status

# Посмотреть все миграции
ls -la prisma/migrations/
```

## 🛠️ Если миграция не применяется

1. Проверьте подключение к БД:
```bash
npx prisma db pull  # Проверит подключение
```

2. Проверьте, что миграция существует:
```bash
ls -la prisma/migrations/20250125000000_add_department/
```

3. Примените вручную (если нужно):
```bash
# Подключитесь к БД и выполните SQL вручную
psql -U postgres -d worktimesk -f prisma/migrations/20250125000000_add_department/migration.sql
```

## 📌 Команды Prisma

```bash
npm run prisma:generate      # Генерация Prisma Client
npm run prisma:migrate       # Создание и применение миграций (dev)
npm run prisma:deploy        # Применение миграций (production)
npm run prisma:studio        # Открыть Prisma Studio (GUI для БД)
```

